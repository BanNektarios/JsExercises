function isSolved([board]) {
    for (x of board) {
        console.log(x)
        if (x.includes(0)) return -1
      }
        // for (item of arr) {
        //     console.log("Second FOR")
        // }
        // if (x.includes(0)) console.log("There is a zero here")
    }
    // if (board.entries().includes(0)) console.log("There is a zero here");
    // const winningComb = new RegExp([])
    // switch(board) {
    //     case 
    // }


let arr1 = {0:[1,0,3,4],1:[1,2,3,4]}
let arr2 = [[1,0,3,4],[1,2,3,4]]

console.log(arr1.values(arr1))